version: 2.1

workflows:
  version: 2
  build:
    jobs:
      - build:
          context:
            - cm-team-github
            - dockerhub-shared

jobs:
  build:
    docker:
      - image: circleci/golang:1.22
    steps:
      - checkout
      - run: go build

